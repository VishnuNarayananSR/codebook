import React from "react";

const App: React.FC = () => {
  return (
    <div>
      <div>
        <textarea cols={100} rows={10}></textarea>
        <br />
        <button>Submit</button>
      </div>
    </div>
  );
};

export default App;
